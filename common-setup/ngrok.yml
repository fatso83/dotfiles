# vim: set sw=2 ts=2:

region: eu
tunnels:
  demo:
    proto: http
    inspect: false
    addr: 9090
    hostname: demo.inconshreveable.com
    auth: demo:secret

  # the http version
  nimble:
    proto: http
    addr: 3000
    subdomain: nimble-ce

  # the https->https version
  nimble-s:
    proto: tls
    addr: 3001
    subdomain: nimble-ce-s
